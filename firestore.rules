rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomCode}/{document=**} {
      allow read, write: if roomCode.matches('^[A-Za-z0-9_-]{4,48}$');
    }
  }
}
